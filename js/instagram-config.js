// Phase 1 placeholder — up to 4 Instagram post/reel URLs. In Phase 2 the
// owner adds these from the admin dashboard and they're stored in Firestore
// (meta/instagramVideos); this hardcoded array is only the seam until then.
const INSTAGRAM_VIDEO_URLS = [null, null, null, null];
